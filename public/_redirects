# Redirects for static HTML structure
# Since we now have static HTML files, we don't need hash redirects

# Redirect contact page without language prefix to English version
/contact /en/contact/ 301

# Fallback for all other routes to 404 page
/* /404.html 404